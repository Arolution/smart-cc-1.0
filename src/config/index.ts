// Re-export hub for config (import with '../config' from pages/components)
// Use explicit .ts extension to avoid resolver edge-cases on some platforms.

export { DEFAULTS } from './defaults.ts';
export { default } from './defaults.ts';